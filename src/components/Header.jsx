
export function Header({type}) { //деструктурезировала объект и использую ключ type для получения значения
    return (
        <>
            <h1>Приклад коду, {type} компонент (без стану)</h1>
        </>
    )

}

//export default Header